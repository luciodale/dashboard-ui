# Project Overview

## Setup Instructions

To get started with the project, follow these steps:

`npm/yarn/bun install` - Install the project dependencies.

`npm/yarn/bun run dev` - Start the development server.

Then, visit `http://localhost:3000/`.